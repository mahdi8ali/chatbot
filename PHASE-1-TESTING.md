# 🧪 اختبار المرحلة 1 - البوت المقيّد

## التحضير قبل الاختبار

### 1. التأكد من التثبيت والإعداد

```powershell
# إذا لم تكن قد ثبتت المكتبات بعد
npm install

# التأكد من إعداد .env.local
# يجب أن يحتوي على:
# OPENAI_API_KEY=sk-proj-xxxxx
# OPENAI_MODEL=gpt-4o
# مفاتيح Supabase الأخرى...
```

### 2. تشغيل المشروع

```powershell
npm run dev
```

انتظر حتى تظهر: `✓ Ready in X.Xs`

---

## 🎯 سيناريوهات الاختبار

### السيناريو 1: التعريف بالذات ✅

**الهدف:** التحقق من أن البوت يعرّف نفسه بشكل صحيح

**الخطوات:**
1. افتح: http://localhost:3000
2. سجل دخول (أو أنشئ حساب جديد)
3. أرسل: **"من أنت؟"**

**النتيجة المتوقعة:**
```
✅ يذكر أنه "مساعد مشاريع الكفيل"
✅ يذكر أنه متخصص في موقع projects.alkafeel.net
✅ يذكر أنه يساعد في المشاريع والفعاليات
```

**النتيجة غير المقبولة:**
```
❌ يقول "أنا GPT-4" أو "نموذج لغوي"
❌ لا يذكر الموقع أو التخصص
```

---

### السيناريو 2: رفض الأسئلة خارج النطاق ❌

**الهدف:** التأكد من أن البوت لا يجيب خارج نطاق الموقع

#### اختبار 2.1: سؤال عام
**أرسل:** "ما هي عاصمة فرنسا؟"

**النتيجة المتوقعة:**
```
✅ يعتذر ويقول أنه مختص بالموقع فقط
✅ يعيد توجيه المستخدم لأسئلة عن المشاريع
✅ لا يذكر "باريس" أو أي معلومة عامة
```

#### اختبار 2.2: سؤال تقني
**أرسل:** "كيف أبرمج موقع بلغة Python؟"

**النتيجة المتوقعة:**
```
✅ يوضح أنه متخصص في موقع المشاريع فقط
✅ لا يقدم نصائح برمجية
```

#### اختبار 2.3: سؤال عن الطقس
**أرسل:** "ما هو الطقس اليوم؟"

**النتيجة المتوقعة:**
```
✅ يرفض ويعيد التوجيه
✅ لا يعطي معلومات عن الطقس
```

---

### السيناريو 3: عدم الاختلاق 🚫

**الهدف:** التأكد من أن البوت لا يخترع معلومات

**أرسل:** "أخبرني عن مشروع رقم 999999"

**النتيجة المتوقعة:**
```
✅ يعتذر ويقول "لا توجد معلومات متاحة"
✅ لا يخترع تفاصيل عن مشروع وهمي
✅ يقترح طرق بحث بديلة
```

**النتيجة غير المقبولة:**
```
❌ يعطي تفاصيل عن مشروع غير موجود
❌ يفترض أو يتخيل معلومات
```

---

### السيناريو 4: الأسئلة داخل النطاق ✅

**الهدف:** التحقق من قبول الأسئلة المتعلقة بالموقع

#### اختبار 4.1: سؤال عام عن المشاريع
**أرسل:** "ما هي المشاريع المتاحة؟"

**النتيجة المتوقعة:**
```
✅ يحاول الإجابة أو يقول "لا توجد معلومات في النظام حالياً"
  (لأن API ليس مربوطاً بعد)
✅ لا يرفض السؤال
✅ يشرح أنه يمكن البحث عن مشاريع
```

#### اختبار 4.2: طلب إحصائيات
**أرسل:** "كم عدد المشاريع النشطة؟"

**النتيجة المتوقعة:**
```
✅ يعتذر ويقول "لم أتمكن من العثور على معلومات حالياً"
✅ يقترح طرق استعلام أخرى
✅ لا يخترع أرقام
```

---

### السيناريو 5: معالجة عدم وجود نتائج 🔍

**الهدف:** اختبار Fallback responses

**أرسل:** "ابحث عن مشاريع التعليم في بغداد"

**النتيجة المتوقعة (حالياً - قبل ربط API):**
```
✅ "عذراً، لم أتمكن من العثور على معلومات..."
✅ يقترح إعادة صياغة السؤال
✅ يقترح البحث بطريقة أخرى
✅ لا يعطي نتائج وهمية
```

---

## 📊 جدول التقييم

| السيناريو | النتيجة | ملاحظات |
|-----------|---------|----------|
| 1. التعريف بالذات | ⬜ نجح / ⬜ فشل | |
| 2.1 رفض سؤال عام | ⬜ نجح / ⬜ فشل | |
| 2.2 رفض سؤال تقني | ⬜ نجح / ⬜ فشل | |
| 2.3 رفض سؤال طقس | ⬜ نجح / ⬜ فشل | |
| 3. عدم الاختلاق | ⬜ نجح / ⬜ فشل | |
| 4.1 قبول سؤال مشاريع | ⬜ نجح / ⬜ فشل | |
| 4.2 قبول طلب إحصائيات | ⬜ نجح / ⬜ فشل | |
| 5. Fallback واضح | ⬜ نجح / ⬜ فشل | |

**النتيجة الإجمالية:** ___ / 8

**التقييم:**
- 8/8 = ممتاز ✅
- 6-7/8 = جيد (يحتاج تحسينات بسيطة)
- 4-5/8 = مقبول (يحتاج مراجعة)
- أقل من 4 = يحتاج إعادة ضبط

---

## 🔧 الاختبار عبر API مباشرة

إذا أردت اختبار الـ endpoint مباشرة:

### استخدام PowerShell:

```powershell
$body = @{
    messages = @(
        @{
            role = "user"
            content = "من أنت؟"
        }
    )
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:3000/api/chat/site" `
  -Method POST `
  -ContentType "application/json" `
  -Body $body
```

### استخدام Postman:

**URL:** `POST http://localhost:3000/api/chat/site`

**Body (JSON):**
```json
{
  "messages": [
    {
      "role": "user",
      "content": "من أنت؟"
    }
  ]
}
```

---

## 🐛 حل المشاكل المحتملة

### المشكلة: "User not found" عند الاختبار من الواجهة

**الحل:**
```
1. اذهب إلى http://localhost:3000/login
2. أنشئ حساب جديد (أي email)
3. سجل دخول
4. جرب الدردشة
```

---

### المشكلة: البوت لا يلتزم بالقيود

**التحقق:**
1. افتح: `app/api/chat/site/route.ts`
2. تأكد من السطر:
   ```typescript
   const systemPrompt = getSiteSystemPrompt()
   ```
3. تأكد من حقن الـ System Prompt في `messagesWithSystem`

**الحل المقترح:**
- جرب تقليل `temperature` إلى 0.5
- تأكد من استخدام `gpt-4o` وليس `gpt-3.5-turbo`

---

### المشكلة: أخطاء في TypeScript

**السبب:** لم يتم تشغيل `npm install` بعد

**الحل:**
```powershell
npm install
```

ثم أعد تشغيل:
```powershell
npm run dev
```

---

## ✅ معايير النجاح

المرحلة 1 تُعتبر ناجحة إذا:

- ✅ البوت يعرّف نفسه بشكل صحيح
- ✅ يرفض 90%+ من الأسئلة خارج النطاق
- ✅ لا يخترع معلومات أبداً
- ✅ Fallback responses واضحة ومفيدة
- ✅ يقبل الأسئلة المتعلقة بالموقع
- ✅ System Prompt لا يظهر في الواجهة
- ✅ لا أخطاء تقنية

---

## 📝 تقرير النتائج

بعد الاختبار، سجل النتائج:

**التاريخ:** ___________

**الاختبارات الناجحة:** ___ / 8

**ملاحظات:**
```
- 
- 
- 
```

**هل المرحلة 1 جاهزة للانتقال للمرحلة 2؟**
⬜ نعم، جاهز
⬜ لا، يحتاج تحسينات (حدد أعلاه)

---

**المرحلة التالية:** ربط REST API وFunction Calling
