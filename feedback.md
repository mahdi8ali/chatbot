المطلوب تنفيذ نظام Feedback + Question Analytics للشات بوت، بحيث نستفيد من أسئلة المستخدمين ونحوّلها إلى رصيد لتحسين البوت وقاعدة البيانات والـ FAQ.

الهدف:
ليس فقط حفظ الأسئلة، بل معرفة:

* ماذا يسأل الناس؟
* هل أجاب البوت بشكل مفيد؟
* ما الأسئلة التي فشل فيها؟
* ما الكلمات العامية المتكررة؟
* ما المواضيع الناقصة في قاعدة البيانات؟
* ما الأسئلة التي تستحق تحويلها إلى FAQ أو Cache؟

---

## 1. حفظ كل سؤال وجواب في قاعدة البيانات

أنشئ جدول باسم مثل:

`chat_logs`

الحقول المقترحة:

```sql
id
session_id
user_question
normalized_question
detected_intent
tool_called
tool_arguments
db_result_ids
db_result_count
final_answer
response_time_ms
model_name
was_tool_used
created_at
```

شرح الحقول:

* `user_question`: السؤال الأصلي كما كتبه المستخدم.
* `normalized_question`: نسخة منظّمة من السؤال إن وجدت، مثل تحويل "هذا مال اعلاف الدواجن شكد صارله؟" إلى "متى افتتح معمل أعلاف الدواجن؟".
* `detected_intent`: نوع السؤال، مثل `project_info`, `project_opening_date`, `faq`, `general`, `medical_warning`.
* `tool_called`: اسم الأداة التي استُخدمت، مثل `search_projects_db`.
* `tool_arguments`: معاملات الأداة.
* `db_result_ids`: أرقام النتائج التي رجعت من قاعدة البيانات.
* `db_result_count`: عدد النتائج.
* `final_answer`: الرد النهائي الذي ظهر للمستخدم.
* `response_time_ms`: زمن الاستجابة.
* `model_name`: اسم الموديل المستخدم.
* `was_tool_used`: هل تم استخدام أداة فعلًا أم لا.

---

## 2. إضافة تقييم بعد كل جواب

بعد ظهور جواب البوت، أظهر للمستخدم زرين واضحين:

* 👍 مفيدة
* 👎 غير مفيدة

يفضل أن تكون صغيرة وأنيقة تحت كل رد من البوت.

عند الضغط على "مفيدة":

* خزّن feedback = `helpful`
* لا تظهر خانة ملاحظة.
* أظهر رسالة صغيرة:
  "شكرًا، تم تسجيل تقييمك."

عند الضغط على "غير مفيدة":

* خزّن feedback = `not_helpful`
* أظهر خانة ملاحظة اختيارية تسأل:
  "ما المشكلة في الإجابة؟"

وتحتها textarea قصيرة، مع placeholder مثل:

"مثلاً: الجواب غير دقيق، لم يفهم سؤالي، لم يجد المعلومة، الإجابة ناقصة..."

مع زر:
"إرسال الملاحظة"

---

## 3. إنشاء جدول feedback

أنشئ جدول باسم مثل:

`chat_feedback`

الحقول المقترحة:

```sql
id
chat_log_id
session_id
rating
feedback_note
feedback_reason
created_at
```

الشرح:

* `chat_log_id`: مربوط بسجل السؤال والجواب في جدول `chat_logs`.
* `session_id`: نفس جلسة المستخدم.
* `rating`: إما `helpful` أو `not_helpful`.
* `feedback_note`: الملاحظة التي يكتبها المستخدم إذا اختار غير مفيدة.
* `feedback_reason`: اختياري، يمكن لاحقًا تحويله إلى تصنيف مثل:

  * `wrong_answer`
  * `not_understood`
  * `missing_info`
  * `too_slow`
  * `bad_language`
  * `other`

مهم:
لا تجعل كتابة الملاحظة إجبارية حتى لا ينزعج المستخدم، لكنها تظهر فقط عند اختيار "غير مفيدة".

---

## 4. واجهة المستخدم المقترحة

تحت كل جواب من البوت:

```text
هل كانت الإجابة مفيدة؟
[👍 مفيدة] [👎 غير مفيدة]
```

إذا اختار "غير مفيدة":

```text
ما المشكلة في الإجابة؟ اختياري
[ textarea ]
[إرسال الملاحظة]
```

بعد الإرسال:

```text
شكرًا، ملاحظتك تساعدنا على تحسين الإجابات.
```

---

## 5. API لحفظ التقييم

أضف endpoint مثل:

`POST /api/chat/feedback`

يرسل:

```json
{
  "chat_log_id": "123",
  "session_id": "abc",
  "rating": "not_helpful",
  "feedback_note": "لم يفهم قصدي، كنت أسأل عن تاريخ الافتتاح"
}
```

ويحفظها في جدول `chat_feedback`.

---

## 6. حماية الخصوصية

قبل حفظ الأسئلة أو الملاحظات، حاول تقليل حفظ المعلومات الحساسة قدر الإمكان.

يفضل إضافة فلتر بسيط لإزالة أو إخفاء:

* أرقام الهواتف
* الإيميلات
* التوكنات
* كلمات السر
* أرقام الهويات إن وجدت

مثال:
بدل حفظ رقم الهاتف كاملًا:
`07701234567`
يحفظ:
`[PHONE]`

وبدل الإيميل:
`test@example.com`
يحفظ:
`[EMAIL]`

---

## 7. لوحة متابعة داخلية أو تقرير أسبوعي

أحتاج لاحقًا صفحة Admin بسيطة أو query تعرض:

* أكثر الأسئلة تكرارًا.
* أكثر الأسئلة التي حصلت على 👎 غير مفيدة.
* الأسئلة التي لم تستدعِ أي أداة.
* الأسئلة التي رجعت `db_result_count = 0`.
* متوسط زمن الاستجابة.
* أكثر intents تكرارًا.
* أكثر الكلمات العامية استخدامًا.
* الأسئلة التي تستحق إضافتها إلى FAQ أو Cache.

---

## 8. تصنيف الأسئلة لاحقًا

بعد حفظ كمية جيدة من الأسئلة، نستطيع تشغيل عملية دورية تقوم بتصنيف الأسئلة إلى مجموعات:

مثلاً:

* أسئلة عن المشاريع.
* أسئلة عن الافتتاحات والتواريخ.
* أسئلة عن الخدمات.
* أسئلة طبية أو طارئة.
* أسئلة لم تُفهم.
* أسئلة متكررة تصلح FAQ.
* أسئلة تحتاج إضافة محتوى جديد في قاعدة البيانات.

هذا لا يجب تنفيذه من البداية بشكل معقد. المهم الآن حفظ البيانات بشكل صحيح.

---

## 9. معيار نجاح التنفيذ

أعتبر التنفيذ ناجحًا إذا تحقق الآتي:

* كل سؤال وجواب يتم حفظه في `chat_logs`.
* كل جواب يظهر تحته تقييم مفيد / غير مفيد.
* عند اختيار غير مفيدة تظهر خانة ملاحظة.
* الملاحظة تُحفظ مرتبطة بنفس السؤال والجواب.
* لا يحدث أي تأثير على سرعة أو عمل الشات بوت.
* لا يتم تخريب منطق الأدوات أو قاعدة البيانات.
* يمكنني لاحقًا استخراج الأسئلة السيئة والمتكررة بسهولة.

---

## 10. المهم جدًا

لا تعدّل منطق OpenAI أو أدوات البحث أثناء تنفيذ هذه الميزة.

هذه الميزة فقط للتسجيل والتحليل والتقييم:

* لا تغيّر system prompt.
* لا تغيّر tool calling.
* لا تغيّر طريقة البحث.
* لا تغيّر طريقة الإجابة.

الهدف الآن مراقبة جودة الشات بوت وجمع بيانات تساعدنا نحسّنه لاحقًا.
