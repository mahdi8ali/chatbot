# 📘 المرحلة 1 - قلب البوت: System Prompt المقيّد

## ✅ ما تم إنجازه

### 1. System Prompt ثابت على السيرفر

**الملف:** `lib/server/system-prompts.ts`

**المميزات:**
- ✅ تعريف البوت: مساعد مشاريع الكفيل لموقع projects.alkafeel.net
- ✅ قواعد صارمة: المصدر الوحيد هو REST API
- ✅ ممنوع المعرفة العامة أو الاختلاق
- ✅ قوالب Fallback واضحة لكل حالة:
  - `FALLBACK_NO_RESULTS` - عند عدم وجود نتائج
  - `FALLBACK_API_ERROR` - عند حدوث خطأ تقني
  - `FALLBACK_OUT_OF_SCOPE` - عند سؤال خارج النطاق
- ✅ رسالة ترحيب `WELCOME_MESSAGE`

---

### 2. Endpoint موحد للشات

**المسار:** `POST /api/chat/site`

**الملف:** `app/api/chat/site/route.ts`

**المميزات:**
- ✅ حقن System Prompt تلقائياً (server-side فقط)
- ✅ استخدام `OPENAI_MODEL` من environment variables
- ✅ معالجة أخطاء شاملة مع fallback responses
- ✅ دعم Streaming للردود
- ✅ إعدادات محسنة:
  - Temperature: 0.7 (افتراضي)
  - Max tokens: 2000
  - Presence penalty: 0.6
  - Frequency penalty: 0.3

---

### 3. أنواع بيانات موحدة

**الملف:** `types/site-chat.ts`

يحتوي على:
- `ChatMessage` - بنية الرسالة
- `SiteChatRequest` - طلب الشات
- `SiteChatResponse` - رد ناجح
- `SiteChatError` - رد خطأ
- `FallbackType` - أنواع الـ fallback
- `ResponseStatus` - حالة الرد

---

### 4. أدوات التحقق والتحليل

**الملف:** `lib/server/chat-validators.ts`

**الدوال المتاحة:**

#### `isQuestionInScope(question: string): boolean`
يفحص ما إذا كان السؤال داخل نطاق البوت

#### `detectUserIntent(question: string): UserIntent`
يحدد نية المستخدم:
- `search_projects` - البحث عن مشاريع
- `project_details` - تفاصيل مشروع
- `project_status` - حالة مشروع
- `statistics` - إحصائيات
- `events` - فعاليات
- `general_help` - مساعدة عامة
- `out_of_scope` - خارج النطاق
- `unknown` - غير واضح

#### `isResponseCompliant(response: string): boolean`
يتحقق من التزام رد AI بالقيود

#### `cleanResponse(response: string): string`
ينظف الرد من عبارات غير مرغوبة

---

## 🎯 كيفية عمل البوت حالياً

### سيناريو 1: سؤال داخل النطاق

**المستخدم:** "ما هي المشاريع المتاحة؟"

**البوت:**
```
بناءً على بيانات النظام، إليك المشاريع المتاحة:
[سيتم ربط API في المرحلة التالية]

حالياً: عذراً، لا توجد معلومات متاحة في النظام...
```

---

### سيناريو 2: سؤال خارج النطاق

**المستخدم:** "ما هو الطقس اليوم؟"

**البوت:**
```
أنا متخصص في الإجابة عن الأسئلة المتعلقة بـ موقع 
projects.alkafeel.net فقط، مثل:

✅ يمكنني مساعدتك في:
- البحث عن المشاريع والبرامج
- معرفة تفاصيل المشاريع وحالاتها
...

كيف يمكنني مساعدتك بخصوص الموقع؟
```

---

### سيناريو 3: خطأ تقني

**الحالة:** فشل الاتصال بـ OpenAI

**البوت:**
```
عذراً، حدث خطأ مؤقت أثناء محاولة الوصول للمعلومات.

يُرجى:
- المحاولة مرة أخرى بعد قليل
- التأكد من صياغة السؤال بوضوح
...
```

---

## 🧪 كيفية الاختبار

### الطريقة 1: من الواجهة (بعد التشغيل)

```powershell
npm run dev
```

افتح: http://localhost:3000

**جرب هذه الأسئلة:**

✅ **داخل النطاق:**
```
- ما هي المشاريع المتاحة؟
- أعطني تفاصيل عن مشروع معين
- كم عدد المشاريع النشطة؟
```

❌ **خارج النطاق:**
```
- ما هو الطقس اليوم؟
- كيف أطبخ الأرز؟
- من هو الرئيس الأمريكي؟
```

---

### الطريقة 2: باستخدام cURL (API مباشرة)

```powershell
curl -X POST http://localhost:3000/api/chat/site `
  -H "Content-Type: application/json" `
  -d '{
    "messages": [
      {
        "role": "user",
        "content": "ما هي المشاريع المتاحة؟"
      }
    ]
  }'
```

---

### الطريقة 3: باستخدام Postman

**URL:** `POST http://localhost:3000/api/chat/site`

**Headers:**
```
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "messages": [
    {
      "role": "user",
      "content": "ابحث عن مشاريع التعليم"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 2000
}
```

---

## 📋 قائمة التحقق

### ✅ يجب أن يتحقق:

- [ ] البوت يعرّف نفسه كـ "مساعد مشاريع الكفيل"
- [ ] البوت لا يجيب على أسئلة خارج نطاق الموقع
- [ ] عند سؤال خارج النطاق، يعيد توجيه المستخدم
- [ ] عند عدم توفر معلومات، لا يخترع بيانات
- [ ] الردود واضحة ومنظمة
- [ ] System Prompt لا يظهر في الواجهة
- [ ] مفاتيح API محمية (server-side فقط)

---

## 🔍 اختبارات موصى بها

### اختبار 1: التعريف بالذات
**السؤال:** "من أنت؟"

**النتيجة المتوقعة:** يقول أنه مساعد لموقع projects.alkafeel.net

---

### اختبار 2: حدود المعرفة
**السؤال:** "ما هي عاصمة فرنسا؟"

**النتيجة المتوقعة:** يرفض ويوجه للاستفسار عن الموقع

---

### اختبار 3: عدم الاختلاق
**السؤال:** "أخبرني عن مشروع رقم 999999"

**النتيجة المتوقعة:** يعتذر ويقول "لا توجد معلومات"

---

### اختبار 4: الالتزام بالنطاق
**السؤال:** "أعطني قائمة بالمشاريع"

**النتيجة المتوقعة:** يحاول الإجابة أو يقول "لم أتمكن من العثور على معلومات في النظام" (لأن API غير مربوط بعد)

---

## 📁 الملفات الجديدة

```
chatbot/
├── app/api/chat/site/
│   └── route.ts                     ⭐ Endpoint موحد للشات
│
├── lib/server/
│   ├── system-prompts.ts            📜 System Prompt المقيد
│   ├── chat-validators.ts           🔍 أدوات التحقق
│   └── site-api-config.ts           ⚙️ (موجود مسبقاً)
│
└── types/
    └── site-chat.ts                 📝 أنواع البيانات
```

---

## 🎯 الخطوة التالية: المرحلة 2

**بعد التأكد من عمل المرحلة 1:**

⏭️ **المرحلة 2 - تكامل REST API:**
- إنشاء service layer للتواصل مع API
- إضافة Function Calling لاختيار endpoints
- ربط البيانات الحقيقية

---

## 🆘 حل المشاكل

### المشكلة: "OpenAI API Key not found"

**الحل:**
```powershell
# تأكد من .env.local
OPENAI_API_KEY=sk-proj-xxxxx
OPENAI_MODEL=gpt-4o
```

---

### المشكلة: البوت يجيب خارج النطاق

**السبب المحتمل:** الـ System Prompt لم يُحقن بشكل صحيح

**التحقق:**
- افتح `app/api/chat/site/route.ts`
- تأكد من استدعاء `getSiteSystemPrompt()`

---

### المشكلة: الردود غير منظمة

**الحل:** عدّل `temperature` في الطلب:
```json
{
  "temperature": 0.5
}
```
(قيمة أقل = ردود أكثر تركيزاً)

---

## 📊 المقاييس المستهدفة

| المقياس | الهدف | الحالي |
|---------|--------|--------|
| دقة الرد داخل النطاق | 95%+ | ⏳ يُختبر |
| رفض الأسئلة خارج النطاق | 90%+ | ⏳ يُختبر |
| وضوح الردود | 4.5/5 | ⏳ يُختبر |
| زمن الاستجابة | <3 ثانية | ⏳ يُختبر |

---

**تاريخ الإنجاز:** 15 فبراير 2026  
**الحالة:** ✅ المرحلة 1 مكتملة - جاهز للاختبار  
**الخطوة التالية:** اختبار شامل ثم الانتقال للمرحلة 2
